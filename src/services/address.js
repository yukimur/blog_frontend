// 根据 process.env.NODE_ENV 切换不同的 baseURL
// const isPro = process.env.NODE_ENV === 'production'

module.exports = {
    // 'apis'：vue.config.js中proxy设置的代理
    // baseURL: 'http://82.156.121.144:8080/',
    baseURL: 'http://yukimura.club',
}